# usage of react hook form
npm i react-hook-form

# Zod for validation schema on client as well on server side ( or when im pulling out data from local storage)

npm i zod

npm i @hookform/resolvers

# now i only use react hook form for registering inputs and all the validation is centralized to zod schema


If you want to use getUserByEmail, you'll need to run it on a backend, and invoke that from your web client.